// ============================================================
// HRFlow — APP.JS
// Entry point — jalankan app saat halaman load
// ============================================================

document.addEventListener('DOMContentLoaded', function() {
  updateBadge();
  loadFromDB();
});
